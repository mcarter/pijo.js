jsio('import Class, bind');
jsio('import jsio.logging');

var logger = jsio.logging.getLogger("pijo.client");

exports.Client = Class(jsio.interfaces.Protocol


var response = function() {
	this.complete.apply(arguments.callee, arguments);
}
response.complete

Class(pijo.Handler, function(supr) {

	this.echo = function(response, args, a,b,c) {
		db.doSomething.cb(bind(this, function(result) {
			response(result);
			response.
		}))
		
		response.error
		sendResponse(args);
	}
}


Class(pijo.client.Client, function(supr) {
	this.init = function() {
		supr([this, 'init', 'echo.pijo']);
	}
	
	this.echo = 

}
